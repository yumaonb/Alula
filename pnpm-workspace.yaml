# pnpm-workspace.yaml —— pnpm v11 起，除认证/源（.npmrc）以外的配置都从这里读。
# 根 package.json 里的 "pnpm" 字段已被 pnpm 忽略，原先的 onlyBuiltDependencies 迁移到下面的 allowBuilds。
# 本项目是单包仓库，不需要 packages 字段：省略即只包含根包。

# 允许执行安装脚本的依赖（默认拒绝，未列出的依赖会被跳过并报 ERR_PNPM_IGNORED_BUILDS）
allowBuilds:
  esbuild: true
  sharp: true
